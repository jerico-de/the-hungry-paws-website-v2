<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <!-- ADMIN TOP BAR -->
    <header class="user-topbar">
      <div class="user-topbar-left">
        <img src="/images/logo.png" alt="The Hungry Paws" class="user-logo" />
        <span class="welcome-text">
          Welcome,
          <strong><%= user.fullName %></strong>
          (Admin)
        </span>
      </div>
    </header>

    <!-- DASHBOARD LAYOUT -->
    <div class="dashboard">
      <!-- SIDEBAR -->
      <aside class="sidebar">
        <button class="sidebar-link active" data-section="profile">Profile</button>
        <button class="sidebar-link" data-section="bookings">Bookings</button>

        <form action="/logout" method="POST" class="logout-form">
          <button type="submit" class="sidebar-link logout-btn">Logout</button>
        </form>
      </aside>

      <!-- MAIN CONTENT -->
      <main class="dashboard-content" id="dashboardContent" data-email="<%= user.email %>">
        <h2>Profile</h2>
        <p>
          <strong>Email:</strong>
          <%= user.email %>
        </p>
        <p>More profile info will go here.</p>
      </main>
    </div>

    <script src="/admin.js"></script>
  </body>
</html>
